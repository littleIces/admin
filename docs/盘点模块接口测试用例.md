# WMS 盘点模块 — 接口测试用例

**文档版本**：v1.0
**生成日期**：2026-04-16
**模块范围**：WMS 仓库管理系统 — 盘点模块全部接口
**API 基础路径**：`/api/services/app/`

> 说明：本文档基于盘点模块 PRD v2.0 生成，覆盖盘点单管理、盘点任务管理、差异单管理、库存调整单管理、盘点设置、盘点计费、库存冻结、盘点记录查询、权限控制共 9 大模块。ABP 框架下 AppService 方法默认映射为 POST 请求，查询类方法（Get/GetAll/GetList）映射为 GET 请求。

---

## 目录

1. [盘点单管理（StockTakingPlan）](#1-盘点单管理stocktakingplan)
2. [盘点任务管理（StockTakingTask）](#2-盘点任务管理stocktakingtask)
3. [盘点执行与扫描（StockTakingExecution）](#3-盘点执行与扫描stocktakingexecution)
4. [差异单管理（StockTakingDifference）](#4-差异单管理stocktakingdifference)
5. [库存调整单管理（InventoryAdjustment）](#5-库存调整单管理inventoryadjustment)
6. [盘点设置（StockTakingSettings）](#6-盘点设置stocktakingsettings)
7. [盘点计费（StockTakingBilling）](#7-盘点计费stocktakingbilling)
8. [库存冻结（InventoryFreeze）](#8-库存冻结inventoryfreeze)
9. [盘点记录查询（StockTakingRecord）](#9-盘点记录查询stocktakingrecord)
10. [权限控制（通用）](#10-权限控制通用)

---

## 1. 盘点单管理（StockTakingPlan）

### 1.1 创建盘点单 — `POST /api/services/app/StockTakingPlan/Create`

#### 正常请求

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SP-C-001 | 创建全仓盘点单（全部必填字段） | POST `/api/services/app/StockTakingPlan/Create` | `{"name":"2026年4月全仓盘点","warehouseInfoId":1,"scopeType":"FullWarehouse","stockTakingDepth":"SKU","stockTakingMethod":"Open","stockTakingGranularity":"Full"}` | 200 | `result.id > 0`，`result.status == "Draft"`（0），`result.planCode` 匹配 `ST+日期+序号` 格式 | P0 |
| SP-C-002 | 创建库区范围盘点单 | POST `.../Create` | `{"name":"A区盘点","warehouseInfoId":1,"scopeType":"ByArea","stockTakingDepth":"Batch","stockTakingMethod":"Blind","stockTakingGranularity":"Full","scopeItems":[{"scopeValue":"101","scopeSecondaryValue":null}]}` | 200 | `result.status == "Draft"`，范围明细正确保存 | P0 |
| SP-C-003 | 创建SKU+库位盘点单 | POST `.../Create` | `{"name":"精确盘点","warehouseInfoId":1,"scopeType":"ByPosition","stockTakingDepth":"SKU","stockTakingMethod":"Open","stockTakingGranularity":"Full","scopeItems":[{"scopeValue":"A-01-01","scopeSecondaryValue":"SKU001","scopeName":"产品A"}]}` | 200 | `result.status == "Draft"` | P0 |
| SP-C-004 | 创建货主范围盘点单 | POST `.../Create` | `{"name":"货主X盘点","warehouseInfoId":1,"scopeType":"ByOwner","stockTakingDepth":"SKU","stockTakingMethod":"Open","stockTakingGranularity":"Full","scopeItems":[{"scopeValue":"2001"}]}` | 200 | 创建成功 | P1 |
| SP-C-005 | 创建按SKU盘点单（指定货主） | POST `.../Create` | `{"name":"SKU盘点","warehouseInfoId":1,"scopeType":"BySKU","stockTakingDepth":"Batch","stockTakingMethod":"Blind","stockTakingGranularity":"Full","scopeItems":[{"scopeValue":"SKU001","scopeSecondaryValue":"2001","scopeName":"货主A"}]}` | 200 | 创建成功 | P1 |
| SP-C-006 | 创建盘点单含所有可选字段 | POST `.../Create` | `{"name":"完整盘点","warehouseInfoId":1,"scopeType":"FullWarehouse","stockTakingDepth":"SKU","stockTakingMethod":"Open","stockTakingGranularity":"Sample50","plannedStartTime":"2026-04-20T08:00:00","plannedEndTime":"2026-04-22T18:00:00","isFreezeInventory":true,"excludeRecentDays":7}` | 200 | 所有字段正确存储 | P1 |
| SP-C-007 | 创建盲盘+批次级+抽查50%盘点单 | POST `.../Create` | `{"name":"抽查盘点","warehouseInfoId":1,"scopeType":"FullWarehouse","stockTakingDepth":"Batch","stockTakingMethod":"Blind","stockTakingGranularity":"Sample50"}` | 200 | 创建成功 | P1 |

#### 参数边界

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SP-C-008 | 盘点单名称最大长度 | POST `.../Create` | `name` = 200字符字符串（假设最大长度200） | 200 | 创建成功 | P2 |
| SP-C-009 | 盘点单名称最小长度（1字符） | POST `.../Create` | `{"name":"X",...}` | 200 | 创建成功 | P2 |
| SP-C-010 | 计划时间：开始时间=结束时间 | POST `.../Create` | `plannedStartTime == plannedEndTime` | 200 | 创建成功或业务提示 | P2 |
| SP-C-011 | 范围明细为空列表（全仓模式无需明细） | POST `.../Create` | `scopeType=FullWarehouse`，`scopeItems=[]` | 200 | 创建成功 | P1 |
| SP-C-012 | 范围明细包含大量库位（500个） | POST `.../Create` | `scopeType=ByPosition`，`scopeItems` 含500条 | 200 | 创建成功，性能可接受 | P2 |

#### 非法参数

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SP-C-013 | 缺少必填字段 name | POST `.../Create` | `{"warehouseInfoId":1,"scopeType":"FullWarehouse","stockTakingDepth":"SKU","stockTakingMethod":"Open","stockTakingGranularity":"Full"}` | 400/500 | 错误信息提示 name 不能为空 | P0 |
| SP-C-014 | 缺少必填字段 warehouseInfoId | POST `.../Create` | `name` 有值，`warehouseInfoId` 缺失 | 400/500 | 错误信息提示仓库不能为空 | P0 |
| SP-C-015 | 缺少必填字段 scopeType | POST `.../Create` | `scopeType` 缺失 | 400/500 | 错误提示 | P0 |
| SP-C-016 | 缺少必填字段 stockTakingDepth | POST `.../Create` | `stockTakingDepth` 缺失 | 400/500 | 错误提示 | P0 |
| SP-C-017 | 缺少必填字段 stockTakingMethod | POST `.../Create` | `stockTakingMethod` 缺失 | 400/500 | 错误提示 | P0 |
| SP-C-018 | 缺少必填字段 stockTakingGranularity | POST `.../Create` | `stockTakingGranularity` 缺失 | 400/500 | 错误提示 | P0 |
| SP-C-019 | warehouseInfoId 不存在 | POST `.../Create` | `warehouseInfoId: 999999` | 500 | 业务错误，仓库不存在 | P1 |
| SP-C-020 | scopeType 传入非法枚举值 | POST `.../Create` | `"scopeType":"InvalidType"` | 400 | 参数校验失败 | P1 |
| SP-C-021 | stockTakingDepth 传入非法枚举值 | POST `.../Create` | `"stockTakingDepth":"InvalidDepth"` | 400 | 参数校验失败 | P1 |
| SP-C-022 | stockTakingMethod 传入非法枚举值 | POST `.../Create` | `"stockTakingMethod":"InvalidMethod"` | 400 | 参数校验失败 | P1 |
| SP-C-023 | name 为空字符串 | POST `.../Create` | `"name":""` | 400/500 | 名称不能为空 | P1 |
| SP-C-024 | name 传 null | POST `.../Create` | `"name":null` | 400/500 | 名称不能为空 | P1 |
| SP-C-025 | warehouseInfoId 传负数 | POST `.../Create` | `"warehouseInfoId":-1` | 400/500 | 非法参数 | P2 |
| SP-C-026 | warehouseInfoId 传字符串类型 | POST `.../Create` | `"warehouseInfoId":"abc"` | 400 | 类型不匹配 | P2 |
| SP-C-027 | plannedStartTime 格式非法 | POST `.../Create` | `"plannedStartTime":"not-a-date"` | 400 | 日期格式无效 | P2 |
| SP-C-028 | plannedEndTime 早于 plannedStartTime | POST `.../Create` | `plannedStartTime` > `plannedEndTime` | 400/500 | 结束时间不能早于开始时间 | P1 |

#### 业务异常

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SP-C-029 | 按SKU盘点不指定货主 | POST `.../Create` | `scopeType=BySKU`，`scopeItems[0].scopeSecondaryValue` 为空 | 500 | 错误提示"按SKU盘点必须指定货主" | P0 |
| SP-C-030 | ByPosition范围含不属于当前仓库的库位 | POST `.../Create` | `scopeItems[0].scopeValue="Z-99-99"`（不存在） | 500 | 跳过无效库位或报错 | P1 |
| SP-C-031 | ByArea的ScopeValue传名称而非数字ID（Bug#3） | POST `.../Create` | `scopeType=ByArea`，`scopeItems[0].scopeValue="A区"` | 500 | 应提示需传数字ID，而非静默失败 | P1 |
| SP-C-032 | ByOwner的ScopeValue传名称而非数字ID（Bug#3） | POST `.../Create` | `scopeType=ByOwner`，`scopeItems[0].scopeValue="张三"` | 500 | 应提示需传数字ID | P1 |
| SP-C-033 | 范围内库位全部在活跃盘点中 | POST `.../Create` | 指定的所有库位都在进行中的盘点任务中 | 500 | 提示无可用库位或允许创建但范围为空 | P1 |

---

### 1.2 编辑盘点单 — `POST /api/services/app/StockTakingPlan/Update`

#### 正常请求

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SP-U-001 | 修改草稿盘点单名称 | POST `.../Update` | `{"id":1,"name":"新名称",...}` | 200 | 名称更新成功 | P0 |
| SP-U-002 | 修改草稿盘点单范围类型 | POST `.../Update` | 修改 `scopeType` 和对应 `scopeItems` | 200 | 范围变更成功 | P1 |
| SP-U-003 | 修改草稿盘点单的方式/深度/粒度 | POST `.../Update` | 修改 `stockTakingMethod`/`stockTakingDepth`/`stockTakingGranularity` | 200 | 更新成功 | P1 |
| SP-U-004 | 修改已驳回盘点单后重新提交准备 | POST `.../Update` | 状态为 `Rejected`(2) 的盘点单修改字段 | 200 | 修改成功 | P1 |

#### 业务异常

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SP-U-005 | 编辑待审核状态盘点单 | POST `.../Update` | 状态为 `PendingApproval`(1) | 500 | 业务错误，当前状态不允许编辑 | P0 |
| SP-U-006 | 编辑已审核状态盘点单 | POST `.../Update` | 状态为 `Approved`(3) | 500 | 当前状态不允许编辑 | P0 |
| SP-U-007 | 编辑进行中状态盘点单 | POST `.../Update` | 状态为 `InProgress`(4) | 500 | 当前状态不允许编辑 | P0 |
| SP-U-008 | 编辑已完成状态盘点单 | POST `.../Update` | 状态为 `Completed`(5) | 500 | 当前状态不允许编辑 | P0 |
| SP-U-009 | 编辑已作废状态盘点单 | POST `.../Update` | 状态为 `Cancelled`(6) | 500 | 当前状态不允许编辑 | P1 |
| SP-U-010 | 编辑不存在的盘点单 | POST `.../Update` | `id: 999999` | 500 | 盘点单不存在 | P1 |

---

### 1.3 提交盘点单审核 — `POST /api/services/app/StockTakingPlan/Submit`

#### 正常请求

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SP-S-001 | 草稿状态提交审核 | POST `.../Submit` | `{"id":1}` | 200 | `status` 变更为 `PendingApproval`(1)，记录提交人和提交时间 | P0 |
| SP-S-002 | 已驳回状态重新提交 | POST `.../Submit` | `{"id":2}`（已驳回盘点单） | 200 | `status` 变更为 `PendingApproval`(1) | P0 |

#### 业务异常

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SP-S-003 | 待审核状态重复提交 | POST `.../Submit` | 状态为 `PendingApproval`(1) | 500 | 当前状态不允许提交 | P1 |
| SP-S-004 | 已审核状态提交 | POST `.../Submit` | 状态为 `Approved`(3) | 500 | 当前状态不允许提交 | P1 |
| SP-S-005 | 进行中状态提交 | POST `.../Submit` | 状态为 `InProgress`(4) | 500 | 当前状态不允许提交 | P1 |
| SP-S-006 | 已作废状态提交 | POST `.../Submit` | 状态为 `Cancelled`(6) | 500 | 当前状态不允许提交 | P1 |
| SP-S-007 | 提交不存在的盘点单 | POST `.../Submit` | `{"id":999999}` | 500 | 盘点单不存在 | P1 |

---

### 1.4 审核盘点单 — `POST /api/services/app/StockTakingPlan/Approve`

#### 正常请求

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SP-A-001 | 审核通过 | POST `.../Approve` | `{"id":1,"isApproved":true}` | 200 | `status` → `Approved`(3)，记录审核人、审核时间 | P0 |
| SP-A-002 | 审核驳回（含驳回原因） | POST `.../Approve` | `{"id":1,"isApproved":false,"rejectReason":"范围不明确，请补充库区信息"}` | 200 | `status` → `Rejected`(2)，驳回原因保存 | P0 |

#### 非法参数

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SP-A-003 | 驳回不填写原因 | POST `.../Approve` | `{"id":1,"isApproved":false,"rejectReason":""}` | 500 | 驳回时必须填写原因 | P0 |
| SP-A-004 | 驳回原因为null | POST `.../Approve` | `{"id":1,"isApproved":false,"rejectReason":null}` | 500 | 驳回时必须填写原因 | P1 |

#### 业务异常

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SP-A-005 | 非待审核状态审核 | POST `.../Approve` | 状态为 `Draft`(0) 的盘点单 | 500 | 当前状态不允许审核 | P0 |
| SP-A-006 | 已审核状态重复审核 | POST `.../Approve` | 状态为 `Approved`(3) | 500 | 不允许重复审核 | P1 |
| SP-A-007 | 进行中状态审核 | POST `.../Approve` | 状态为 `InProgress`(4) | 500 | 当前状态不允许审核 | P1 |
| SP-A-008 | 无审核权限用户审核 | POST `.../Approve` | 无 `Pages.StockTakingPlan.Approve` 权限的用户 | 403 | 权限不足 | P0 |

---

### 1.5 生成盘点任务 — `POST /api/services/app/StockTakingPlan/GenerateTasks`

#### 正常请求

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SP-G-001 | 已审核盘点单生成任务（按库区拆分） | POST `.../GenerateTasks` | `{"id":1,"splitDimensions":["ByArea"],"maxPositionsPerTask":50,"maxSKUCountPerTask":100,"maxSKUCategoriesPerTask":50}` | 200 | 盘点单 `status` → `InProgress`(4)，返回生成的任务列表，每个任务含盘点明细，库位标记为"盘点中" | P0 |
| SP-G-002 | 按货主拆分生成任务 | POST `.../GenerateTasks` | `{"id":1,"splitDimensions":["ByOwner"]}` | 200 | 不同货主独立生成任务 | P0 |
| SP-G-003 | 按巷道拆分 | POST `.../GenerateTasks` | `{"id":1,"splitDimensions":["ByAisle"]}` | 200 | 按巷道分组 | P1 |
| SP-G-004 | 按库位拆分（最细粒度） | POST `.../GenerateTasks` | `{"id":1,"splitDimensions":["ByPosition"]}` | 200 | 每个库位一个任务 | P1 |
| SP-G-005 | 多维度组合拆分 | POST `.../GenerateTasks` | `{"id":1,"splitDimensions":["ByArea","ByOwner"]}` | 200 | 按库区+货主交叉拆分 | P1 |
| SP-G-006 | IsFreezeInventory=true 时冻结库存 | POST `.../GenerateTasks` | 盘点单 `isFreezeInventory=true` | 200 | 库存被冻结，`InventoryFreezeInfo` 记录创建 | P0 |
| SP-G-007 | IsFreezeInventory=false 时不冻结 | POST `.../GenerateTasks` | 盘点单 `isFreezeInventory=false` | 200 | 库存不冻结，出入库正常 | P1 |
| SP-G-008 | AutoCreateSnapshot=true 创建快照 | POST `.../GenerateTasks` | 设置 `AutoCreateSnapshot=true` | 200 | 盘点明细创建且 `InventorySnapshotInfoId` 非空 | P1 |
| SP-G-009 | AutoCreateSnapshot=false 不创建快照 | POST `.../GenerateTasks` | 设置 `AutoCreateSnapshot=false` | 200 | 盘点明细仍正常创建，`InventorySnapshotInfoId` 为空 | P1 |

#### 参数边界

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SP-G-010 | 单任务最大库位数限制触发拆分 | POST `.../GenerateTasks` | `maxPositionsPerTask=10`，范围含30个库位 | 200 | 至少拆分为3个任务 | P1 |
| SP-G-011 | 单任务最大SKU总数量限制触发拆分 | POST `.../GenerateTasks` | `maxSKUCountPerTask=100`，超出 | 200 | 自动拆分 | P2 |
| SP-G-012 | 单任务最大SKU品种数限制触发拆分 | POST `.../GenerateTasks` | `maxSKUCategoriesPerTask=20`，超出 | 200 | 自动拆分 | P2 |
| SP-G-013 | 抽查50%模式生成 | POST `.../GenerateTasks` | 粒度=`Sample50` | 200 | 任务仅包含约50%库位 | P1 |

#### 业务异常

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SP-G-014 | 非已审核状态生成任务 | POST `.../GenerateTasks` | 盘点单 `status=Draft`(0) | 500 | 当前状态不允许生成任务 | P0 |
| SP-G-015 | 进行中状态重复生成任务 | POST `.../GenerateTasks` | 盘点单 `status=InProgress`(4) | 500 | 不允许重复生成 | P0 |
| SP-G-016 | 范围内无可用库存 | POST `.../GenerateTasks` | 范围内库位均无库存 | 500 | 提示无可用库存 | P1 |
| SP-G-017 | ByOwner拆分时无主库存丢失（Bug#1） | POST `.../GenerateTasks` | 按货主拆分，存在 `OwnerUserId=null` 的库存 | 200 | 无主库存也应纳入任务（已知缺陷，预期包含 null 组） | P0 |
| SP-G-018 | BySKU拆分时SKU=null丢失（Bug#2） | POST `.../GenerateTasks` | 按SKU拆分，存在 `SKU=null` 的库存 | 200 | 无SKU库存也应纳入任务（已知缺陷） | P0 |
| SP-G-019 | TotalPositionCount/TotalSKUCount为0（Bug#4） | POST `.../GenerateTasks` | 正常生成任务 | 200 | `TotalPositionCount` > 0，`TotalSKUCount` > 0（已知缺陷，当前为0） | P0 |
| SP-G-020 | 并发生成任务 | POST `.../GenerateTasks` | 两个请求同时生成同一盘点单的任务 | 200/500 | 仅一个成功，另一个拒绝（防并发） | P1 |

---

### 1.6 预览任务生成 — `POST /api/services/app/StockTakingPlan/PreviewTasks`

#### 正常请求

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SP-P-001 | 预览任务分布 | POST `.../PreviewTasks` | `{"id":1,"splitDimensions":["ByArea"]}` | 200 | 返回预览结果（任务数量、分布），不创建实际任务 | P0 |
| SP-P-002 | 抽查50%预览+手动调整 | POST `.../PreviewTasks` | 粒度=`Sample50` | 200 | 展示随机50%库位列表，支持调整 | P1 |

---

### 1.7 作废盘点单 — `POST /api/services/app/StockTakingPlan/Cancel`

#### 正常请求

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SP-X-001 | 草稿状态作废 | POST `.../Cancel` | `{"id":1}` | 200 | `status` → `Cancelled`(6) | P1 |
| SP-X-002 | 已驳回状态作废 | POST `.../Cancel` | `{"id":2}`（已驳回） | 200 | `status` → `Cancelled`(6) | P1 |
| SP-X-003 | 进行中状态作废（管理员） | POST `.../Cancel` | `{"id":3}`（进行中，管理员操作） | 200 | 盘点单→已作废，所有任务→已取消，库位恢复"正常"，库存解冻 | P0 |

#### 业务异常

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SP-X-004 | 已完成状态作废 | POST `.../Cancel` | 状态为 `Completed`(5) | 500 | 已完成的盘点单不允许作废 | P1 |
| SP-X-005 | 已作废状态重复作废 | POST `.../Cancel` | 状态为 `Cancelled`(6) | 500 | 不允许重复作废 | P2 |
| SP-X-006 | 进行中非管理员作废 | POST `.../Cancel` | 非管理员角色尝试作废进行中盘点单 | 403/500 | 权限不足 | P1 |

---

### 1.8 查询盘点单列表 — `GET /api/services/app/StockTakingPlan/GetAll`

#### 正常请求

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SP-L-001 | 无筛选条件查询列表 | GET `.../GetAll` | `?skipCount=0&maxResultCount=10` | 200 | 返回分页列表，含 `totalCount`、`items`（含统计字段） | P0 |
| SP-L-002 | 按盘点单号模糊搜索 | GET `.../GetAll` | `?keyword=ST2026` | 200 | 仅返回单号匹配的记录 | P0 |
| SP-L-003 | 按SKU/产品名称模糊搜索 | GET `.../GetAll` | `?skuKeyword=SKU001` | 200 | 返回包含该SKU的盘点单 | P1 |
| SP-L-004 | 按仓库精确筛选 | GET `.../GetAll` | `?warehouseInfoId=1` | 200 | 仅返回该仓库的盘点单 | P1 |
| SP-L-005 | 按盘点状态多选筛选 | GET `.../GetAll` | `?statuses=0,1,4` | 200 | 仅返回草稿/待审核/进行中 | P0 |
| SP-L-006 | 按范围类型多选筛选 | GET `.../GetAll` | `?scopeTypes=FullWarehouse,ByArea` | 200 | 返回匹配记录 | P1 |
| SP-L-007 | 按创建时间范围筛选 | GET `.../GetAll` | `?creationTimeStart=2026-04-01&creationTimeEnd=2026-04-30` | 200 | 返回该时间范围内创建的记录 | P1 |
| SP-L-008 | 按计划盘点时间范围筛选 | GET `.../GetAll` | `?plannedStartTimeStart=2026-04-15&plannedStartTimeEnd=2026-04-20` | 200 | 返回匹配记录 | P2 |
| SP-L-009 | 按冻结状态筛选 | GET `.../GetAll` | `?freezeStatuses=Frozen` | 200 | 返回已冻结的盘点单 | P2 |
| SP-L-010 | 按盘点方式筛选 | GET `.../GetAll` | `?stockTakingMethods=Blind` | 200 | 仅返回盲盘 | P2 |

#### 参数边界

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SP-L-011 | 分页 skipCount=0, maxResultCount=1 | GET `.../GetAll` | `?skipCount=0&maxResultCount=1` | 200 | 返回1条 | P2 |
| SP-L-012 | 分页越界（skipCount超过总数） | GET `.../GetAll` | `?skipCount=99999&maxResultCount=10` | 200 | `items` 为空，`totalCount` 正确 | P2 |
| SP-L-013 | maxResultCount=0 | GET `.../GetAll` | `?maxResultCount=0` | 200/400 | 返回空或参数错误 | P2 |

#### 非法参数

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SP-L-014 | skipCount 传负数 | GET `.../GetAll` | `?skipCount=-1` | 400 | 参数无效 | P2 |
| SP-L-015 | maxResultCount 传负数 | GET `.../GetAll` | `?maxResultCount=-1` | 400 | 参数无效 | P2 |

---

### 1.9 导入盘点范围 — `POST /api/services/app/StockTakingPlan/ImportScope`

#### 正常请求

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SP-I-001 | ByPosition模式导入（含库位编码） | POST `.../ImportScope` | Excel 文件，列：库位编码(必填)、产品SKU(选填)、货主名称(选填) | 200 | 导入成功，返回成功/失败条数 | P0 |
| SP-I-002 | BySKU模式导入（含SKU+货主） | POST `.../ImportScope` | Excel 文件，列：产品SKU(必填)、货主名称(必填) | 200 | 导入成功 | P0 |

#### 业务异常

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SP-I-003 | ByPosition模式库位编码为空 | POST `.../ImportScope` | Excel 中库位编码列为空 | 200 | 该行跳过并报错 | P1 |
| SP-I-004 | ByPosition模式库位不属于当前仓库 | POST `.../ImportScope` | Excel 中库位编码不存在于目标仓库 | 200 | 该行跳过并报错，有效行正常导入 | P1 |
| SP-I-005 | BySKU模式货主名称为空 | POST `.../ImportScope` | Excel 中货主名称列为空 | 200 | 该行跳过并报错 | P1 |
| SP-I-006 | BySKU模式SKU为空 | POST `.../ImportScope` | Excel 中产品SKU列为空 | 200 | 该行跳过并报错 | P1 |
| SP-I-007 | BySKU模式货主不存在 | POST `.../ImportScope` | 货主名称在系统中不存在 | 200 | 该行跳过并报错 | P1 |
| SP-I-008 | 上传非Excel文件 | POST `.../ImportScope` | 上传 .txt 或 .pdf 文件 | 400/500 | 文件格式不支持 | P1 |
| SP-I-009 | 上传空Excel文件 | POST `.../ImportScope` | 空的 .xlsx 文件 | 200/500 | 提示无有效数据 | P2 |

---

### 1.10 导出盘点单列表 — `GET /api/services/app/StockTakingPlan/Export`

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SP-E-001 | 按当前筛选条件导出 | GET `.../Export` | 与列表相同筛选参数 | 200 | 返回 Excel 文件流 | P2 |

---

## 2. 盘点任务管理（StockTakingTask）

### 2.1 分配任务 — `POST /api/services/app/StockTakingTask/Assign`

#### 正常请求

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| ST-AS-001 | 单个任务分配执行人 | POST `.../Assign` | `{"taskId":1,"assigneeUserId":100}` | 200 | 任务分配成功，`assigneeUserId=100`，状态仍为 `Pending` | P0 |
| ST-AS-002 | 批量分配任务 | POST `.../BatchAssign` | `{"taskIds":[1,2,3],"assigneeUserId":100}` | 200 | 全部分配成功 | P1 |

#### 业务异常

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| ST-AS-003 | 分配非待领取状态的任务 | POST `.../Assign` | 状态为 `InProgress` 的任务 | 500 | 当前状态不允许分配 | P1 |
| ST-AS-004 | 分配给不存在的用户 | POST `.../Assign` | `assigneeUserId=999999` | 500 | 用户不存在 | P1 |
| ST-AS-005 | 分配不存在的任务 | POST `.../Assign` | `taskId=999999` | 500 | 任务不存在 | P1 |
| ST-AS-006 | 已分配任务重新分配 | POST `.../Assign` | 已有执行人的任务 | 200/500 | 覆盖分配或拒绝 | P2 |

---

### 2.2 领取任务 — `POST /api/services/app/StockTakingTask/Claim`

#### 正常请求

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| ST-CL-001 | 列表领取任务 | POST `.../Claim` | `{"taskId":1}` | 200 | `status` → `InProgress`，记录领取人和领取时间 | P0 |
| ST-CL-002 | 扫码领取任务 | POST `.../ClaimByPosition` | `{"positionCode":"A-01-01"}` | 200 | 系统匹配对应待领取任务并完成领取 | P0 |

#### 业务异常

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| ST-CL-003 | 超出任务持有上限 | POST `.../Claim` | 执行人已持有3个（默认上限）未完成任务 | 500 | "已达任务上限，请先完成已有任务" | P0 |
| ST-CL-004 | 领取非待领取状态的任务 | POST `.../Claim` | 状态为 `InProgress` 的任务 | 500 | 当前状态不允许领取 | P1 |
| ST-CL-005 | 领取已取消的任务 | POST `.../Claim` | 状态为 `Cancelled` 的任务 | 500 | 任务已取消 | P1 |
| ST-CL-006 | 扫码无对应任务的库位 | POST `.../ClaimByPosition` | `{"positionCode":"Z-99-99"}` | 500 | 该库位无待领取的盘点任务 | P1 |
| ST-CL-007 | 领取分配给其他人的任务 | POST `.../Claim` | 任务已分配给用户B，用户A尝试领取 | 500 | 该任务已分配给其他人 | P1 |
| ST-CL-008 | 领取不存在的任务 | POST `.../Claim` | `taskId=999999` | 500 | 任务不存在 | P1 |

---

### 2.3 提交任务 — `POST /api/services/app/StockTakingTask/Submit`

#### 正常请求

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| ST-SM-001 | 提交无差异任务（全部E=0） | POST `.../Submit` | `{"taskId":1}` | 200 | 不生成差异单，任务 `status` → `Completed`(4) | P0 |
| ST-SM-002 | 提交有差异任务（存在E≠0） | POST `.../Submit` | `{"taskId":2}` | 200 | 生成差异单（`PendingApproval`），统计盘盈/盘亏数量 | P0 |
| ST-SM-003 | 部分录入后强制提交 | POST `.../Submit` | `{"taskId":3,"isForce":true}` | 200 | 未录入项 `IsCounted=false`，差异单标注"未盘" | P1 |

#### 业务异常

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| ST-SM-004 | 提交非进行中状态的任务 | POST `.../Submit` | 状态为 `Pending` 的任务 | 500 | 当前状态不允许提交 | P1 |
| ST-SM-005 | 提交已完成的任务 | POST `.../Submit` | 状态为 `Completed` | 500 | 不允许重复提交 | P1 |
| ST-SM-006 | 提交已取消的任务 | POST `.../Submit` | 状态为 `Cancelled` | 500 | 任务已取消 | P1 |
| ST-SM-007 | 全部未录入提交（非强制） | POST `.../Submit` | 所有明细均未录入，非强制提交 | 500 | 存在未录入的盘点明细，请先录入或使用强制提交 | P1 |

---

### 2.4 强制完成任务 — `POST /api/services/app/StockTakingTask/ForceComplete`

#### 正常请求

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| ST-FC-001 | 管理员强制完成进行中任务 | POST `.../ForceComplete` | `{"taskId":1}` | 200 | 未录入明细 `IsCounted=false`、`ActualQuantity=null`，任务→已完成，差异单中标注"未盘" | P1 |

#### 业务异常

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| ST-FC-002 | 非管理员强制完成 | POST `.../ForceComplete` | 无 `Pages.StockTakingTask.ForceComplete` 权限 | 403 | 权限不足 | P1 |
| ST-FC-003 | 强制完成已完成的任务 | POST `.../ForceComplete` | 状态为 `Completed` | 500 | 不允许操作 | P2 |
| ST-FC-004 | 强制完成待领取的任务 | POST `.../ForceComplete` | 状态为 `Pending` | 500 | 当前状态不允许强制完成 | P2 |

---

### 2.5 取消任务 — `POST /api/services/app/StockTakingTask/Cancel`

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| ST-CA-001 | 取消待领取任务 | POST `.../Cancel` | `{"taskId":1}` | 200 | `status` → `Cancelled`，库位恢复正常 | P1 |
| ST-CA-002 | 取消进行中任务 | POST `.../Cancel` | `{"taskId":2}` | 200 | 任务取消，已录入数据保留 | P1 |
| ST-CA-003 | 取消已完成任务 | POST `.../Cancel` | 状态为 `Completed` | 500 | 已完成任务不允许取消 | P1 |
| ST-CA-004 | 无权限取消 | POST `.../Cancel` | 无 `Pages.StockTakingTask.Cancel` 权限 | 403 | 权限不足 | P1 |

---

### 2.6 标记紧急 — `POST /api/services/app/StockTakingTask/MarkUrgent`

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| ST-MU-001 | 标记任务为紧急 | POST `.../MarkUrgent` | `{"taskId":1,"isUrgent":true}` | 200 | `IsUrgent=true` | P2 |
| ST-MU-002 | 取消紧急标记 | POST `.../MarkUrgent` | `{"taskId":1,"isUrgent":false}` | 200 | `IsUrgent=false` | P2 |

---

### 2.7 查询任务列表 — `GET /api/services/app/StockTakingTask/GetAll`

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| ST-L-001 | 无条件查询 | GET `.../GetAll` | `?skipCount=0&maxResultCount=10` | 200 | 返回分页列表（含轮次标签、父任务编号） | P0 |
| ST-L-002 | 按盘点单筛选 | GET `.../GetAll` | `?stockTakingPlanId=1` | 200 | 仅返回该盘点单关联的任务 | P1 |
| ST-L-003 | 按任务状态筛选 | GET `.../GetAll` | `?statuses=0,1` | 200 | 返回待领取+进行中 | P1 |
| ST-L-004 | 按轮次筛选 | GET `.../GetAll` | `?round=2` | 200 | 仅返回复盘任务 | P2 |
| ST-L-005 | 按执行人筛选 | GET `.../GetAll` | `?assigneeUserId=100` | 200 | 仅返回该执行人的任务 | P2 |

---

### 2.8 获取任务详情 — `GET /api/services/app/StockTakingTask/Get`

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| ST-D-001 | 获取任务详情含明细和统计 | GET `.../Get` | `?id=1` | 200 | 返回任务基本信息 + 盘点明细列表 + 进度统计（总库位/已盘/总SKU/已盘SKU/差异数） | P0 |
| ST-D-002 | 获取含任务链的详情 | GET `.../Get` | `?id=8`（复盘任务） | 200 | 包含 `parentTaskId` 和完整任务链 | P1 |
| ST-D-003 | 获取不存在的任务 | GET `.../Get` | `?id=999999` | 500 | 任务不存在 | P1 |

---

## 3. 盘点执行与扫描（StockTakingExecution）

### 3.1 录入盘点数据 — `POST /api/services/app/StockTakingExecution/Input`

#### 正常请求

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SE-I-001 | 明盘模式录入正常SKU | POST `.../Input` | `{"taskId":1,"sku":"SKU001","actualQuantity":100,"dataSource":"PDA"}` | 200 | 录入成功，返回 `systemQuantity(C)`、`differenceQuantity(E=D-C)` | P0 |
| SE-I-002 | 盲盘模式录入 | POST `.../Input` | `{"taskId":2,"sku":"SKU001","actualQuantity":95,"dataSource":"PDA"}` | 200 | 录入成功，响应中**不包含** `systemQuantity` 字段 | P0 |
| SE-I-003 | Web页面手动录入 | POST `.../Input` | `{"taskId":1,"sku":"SKU001","actualQuantity":100,"dataSource":"Web"}` | 200 | 录入成功 | P1 |
| SE-I-004 | 录入新增SKU（库位无记录但产品库存在） | POST `.../Input` | `{"taskId":1,"sku":"SKU_NEW","actualQuantity":5,"dataSource":"PDA"}` | 200 | 录入成功，`isNewSKU=true`，差异单标注"新增" | P1 |
| SE-I-005 | 录入实盘数量为0 | POST `.../Input` | `{"taskId":1,"sku":"SKU001","actualQuantity":0}` | 200 | 录入成功，`E = 0 - C`，标记为盘亏 | P1 |
| SE-I-006 | 批次级录入（指定批次号） | POST `.../Input` | `{"taskId":1,"sku":"SKU001","batchNo":"BATCH20260401","actualQuantity":50}` | 200 | 按批次级记录 | P1 |

#### 参数边界

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SE-I-007 | actualQuantity=1（最小正数） | POST `.../Input` | `actualQuantity=1` | 200 | 录入成功 | P2 |
| SE-I-008 | actualQuantity 极大值 | POST `.../Input` | `actualQuantity=999999999` | 200 | 录入成功（或业务上限校验） | P2 |
| SE-I-009 | actualQuantity=0 | POST `.../Input` | `actualQuantity=0` | 200 | 录入成功，表示该SKU实物为0 | P1 |

#### 非法参数

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SE-I-010 | actualQuantity 为负数 | POST `.../Input` | `actualQuantity=-1` | 400/500 | 数量不能为负数 | P0 |
| SE-I-011 | actualQuantity 为小数 | POST `.../Input` | `actualQuantity=10.5` | 400/500 | 数量必须为整数（视业务规则） | P1 |
| SE-I-012 | sku 为空 | POST `.../Input` | `sku=""` | 400/500 | SKU不能为空 | P0 |
| SE-I-013 | sku 为null | POST `.../Input` | `sku=null` | 400/500 | SKU不能为空 | P1 |
| SE-I-014 | taskId 为空/0 | POST `.../Input` | `taskId=0` | 400/500 | 任务ID无效 | P1 |

#### 业务异常

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SE-I-015 | SKU不在产品库中 | POST `.../Input` | `sku="UNKNOWN_SKU"` | 500 | "该SKU未注册，请先完成产品注册后再盘点" | P0 |
| SE-I-016 | 任务非进行中状态录入 | POST `.../Input` | 任务 `status=Pending` | 500 | 当前状态不允许录入 | P0 |
| SE-I-017 | 录入已完成的任务 | POST `.../Input` | 任务 `status=Completed` | 500 | 任务已完成，不允许录入 | P1 |
| SE-I-018 | 录入已取消的任务 | POST `.../Input` | 任务 `status=Cancelled` | 500 | 任务已取消 | P1 |
| SE-I-019 | SKU不属于当前任务的盘点范围 | POST `.../Input` | 录入的SKU不在任务明细中且非新增场景 | 200/500 | 视业务规则允许新增或拒绝 | P2 |
| SE-I-020 | 多批次SKU未指定批次号 | POST `.../Input` | 同SKU有多批次，不传 `batchNo` | 500 | 该SKU有多个批次，请选择具体批次 | P1 |

---

### 3.2 Excel批量导入盘点数据 — `POST /api/services/app/StockTakingExecution/Import`

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SE-IM-001 | 正常批量导入 | POST `.../Import` | Excel文件 + `taskId` | 200 | 返回成功/失败统计 | P1 |
| SE-IM-002 | 导入含无效SKU的行 | POST `.../Import` | 部分行SKU不存在 | 200 | 无效行跳过并报错，有效行录入 | P1 |
| SE-IM-003 | 导入空文件 | POST `.../Import` | 空Excel | 200/500 | 提示无有效数据 | P2 |

---

## 4. 差异单管理（StockTakingDifference）

### 4.1 获取差异单列表 — `GET /api/services/app/StockTakingDifference/GetAll`

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SD-L-001 | 查询差异单列表 | GET `.../GetAll` | `?skipCount=0&maxResultCount=10` | 200 | 返回分页列表（含盘点单号、任务号、轮次、统计数据） | P0 |
| SD-L-002 | 按盘点单号搜索 | GET `.../GetAll` | `?planCode=ST20260416001` | 200 | 返回匹配记录 | P1 |
| SD-L-003 | 按差异单状态筛选 | GET `.../GetAll` | `?statuses=PendingApproval` | 200 | 仅返回待审核的差异单 | P1 |

---

### 4.2 获取差异单详情 — `GET /api/services/app/StockTakingDifference/Get`

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SD-D-001 | 查看差异单详情 | GET `.../Get` | `?id=1` | 200 | 包含差异明细列表、汇总统计（DifferenceSKUCount、SurplusSKUCount、LossSKUCount、TotalDifferenceAmount） | P0 |
| SD-D-002 | 差异统计正确性 | GET `.../Get` | `?id=1` | 200 | `TotalSurplusQuantity=SUM(盘盈E)`，`TotalLossQuantity=SUM(|盘亏E|)`，`TotalDifferenceAmount=SUM(|E|×UnitPrice)` | P0 |
| SD-D-003 | 不存在的差异单 | GET `.../Get` | `?id=999999` | 500 | 差异单不存在 | P1 |

---

### 4.3 审核差异单 — `POST /api/services/app/StockTakingDifference/Approve`

#### 正常请求

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SD-A-001 | 全部明细通过→确认差异 | POST `.../Approve` | `{"id":1,"items":[{"itemId":1,"reviewResult":"Approved"},{"itemId":2,"reviewResult":"Approved"}]}` | 200 | 差异单→`Confirmed`，自动生成调整单 | P0 |
| SD-A-002 | 部分明细驳回重盘→需复盘 | POST `.../Approve` | `{"id":1,"items":[{"itemId":1,"reviewResult":"Approved"},{"itemId":2,"reviewResult":"RejectRecount","reason":"UnCountedNotFound"}]}` | 200 | 差异单→`NeedRecount`，生成复盘任务 | P0 |
| SD-A-003 | 需终盘审核 | POST `.../Approve` | `{"id":1,"items":[{"itemId":1,"reviewResult":"NeedFinal"}]}` | 200 | 差异单→`NeedFinal`，生成终盘任务 | P1 |
| SD-A-004 | 部分认可账面+部分通过 | POST `.../Approve` | `items` 中含 `AcceptSystem` 和 `Approved` | 200 | 差异单→`Confirmed`，调整单不包含"认可账面"的明细 | P0 |
| SD-A-005 | 审核时填写差异原因分类 | POST `.../Approve` | `items[0].reasonCategory="DamageLoss"`，`items[0].reasonRemark="包装破损"` | 200 | 差异原因正确保存 | P1 |

#### 非法参数

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SD-A-006 | 审核结果传非法枚举 | POST `.../Approve` | `reviewResult="InvalidResult"` | 400 | 参数校验失败 | P1 |
| SD-A-007 | 明细ID不存在 | POST `.../Approve` | `items[0].itemId=999999` | 500 | 明细不存在 | P1 |

#### 业务异常

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SD-A-008 | 存在未处理的"未盘"明细时提交 | POST `.../Approve` | 有 `IsCounted=false` 的明细未在审核项中处理 | 500 | "存在未处理的未盘明细，请先逐条处理" | P0 |
| SD-A-009 | 非待审核状态审核 | POST `.../Approve` | 差异单已为 `Confirmed` 状态 | 500 | 当前状态不允许审核 | P1 |
| SD-A-010 | 部分明细未处理就提交 | POST `.../Approve` | `items` 列表不包含所有差异明细 | 500 | 所有明细必须处理完毕后才能提交 | P0 |
| SD-A-011 | 无审核权限操作 | POST `.../Approve` | 无 `Pages.StockTakingDifference.Approve` 权限 | 403 | 权限不足 | P0 |

---

### 4.4 生成复盘任务 — `POST /api/services/app/StockTakingDifference/GenerateRecount`

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SD-R-001 | 生成复盘任务 | POST `.../GenerateRecount` | `{"differenceId":1}` | 200 | 生成复盘任务（Round=2），`ParentTaskId` 指向原初盘任务，仅含差异+未盘明细 | P0 |
| SD-R-002 | 验证复盘任务不含无差异项 | POST `.../GenerateRecount` | 差异单含有差异和无差异明细 | 200 | 复盘任务仅包含 `DifferenceType!=None` 和 `IsCounted=false` 的明细 | P1 |
| SD-R-003 | 复盘指派原盘点人 | POST `.../GenerateRecount` | `RecountAssignRule=Original` | 200 | 执行人为原初盘执行人 | P1 |
| SD-R-004 | 复盘指派指定人员 | POST `.../GenerateRecount` | `RecountAssignRule=Specified`，指定人员ID | 200 | 执行人为指定人员 | P2 |

---

### 4.5 生成终盘任务 — `POST /api/services/app/StockTakingDifference/GenerateFinal`

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SD-F-001 | 生成终盘任务 | POST `.../GenerateFinal` | `{"differenceId":1}` | 200 | 生成终盘任务（Round=3） | P1 |
| SD-F-002 | 无权限生成终盘 | POST `.../GenerateFinal` | 无 `Pages.StockTakingDifference.GenerateFinal` 权限 | 403 | 权限不足 | P1 |

---

### 4.6 差异单状态流转验证

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SD-ST-001 | 待审核→已确认（全部通过/认可账面） | — | — | 200 | 状态流转合法 | P0 |
| SD-ST-002 | 待审核→需复盘（存在驳回重盘） | — | — | 200 | 状态流转合法 | P0 |
| SD-ST-003 | 待审核→需终盘 | — | — | 200 | 状态流转合法 | P1 |
| SD-ST-004 | 已确认→已生成调整单 | — | 系统自动生成调整单后 | — | 状态流转合法 | P0 |
| SD-ST-005 | 非法流转：已确认→待审核 | — | 手动尝试 | 500 | 不允许的状态流转 | P1 |
| SD-ST-006 | 非法流转：需复盘→已确认（不经过复盘） | — | 手动尝试 | 500 | 不允许的状态流转 | P1 |

---

### 4.7 导出差异单 — `GET /api/services/app/StockTakingDifference/Export`

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SD-E-001 | 导出差异明细 | GET `.../Export` | `?differenceId=1` | 200 | 返回Excel文件 | P2 |

---

## 5. 库存调整单管理（InventoryAdjustment）

### 5.1 获取调整单列表 — `GET /api/services/app/InventoryAdjustment/GetAll`

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| IA-L-001 | 查询调整单列表 | GET `.../GetAll` | `?skipCount=0&maxResultCount=10` | 200 | 返回分页列表 | P0 |
| IA-L-002 | 按状态筛选 | GET `.../GetAll` | `?statuses=0,1` | 200 | 返回待提交+待审核 | P1 |
| IA-L-003 | 按盘点单号搜索 | GET `.../GetAll` | `?planCode=ST20260416001` | 200 | 返回匹配记录 | P2 |

---

### 5.2 获取调整单详情 — `GET /api/services/app/InventoryAdjustment/Get`

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| IA-D-001 | 查看调整单详情 | GET `.../Get` | `?id=1` | 200 | 含调整明细，每条含 `BeforeQuantity`、`AfterQuantity`、`DifferenceQuantity` | P0 |
| IA-D-002 | 验证调整量一致性 | GET `.../Get` | `?id=1` | 200 | `AfterQuantity = BeforeQuantity + DifferenceQuantity` | P0 |
| IA-D-003 | 验证"认可账面"明细不在调整单 | GET `.../Get` | 差异单中部分"认可账面" | 200 | 调整单不包含认可账面的明细行 | P1 |

---

### 5.3 提交调整单审核 — `POST /api/services/app/InventoryAdjustment/Submit`

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| IA-S-001 | 草稿提交审核 | POST `.../Submit` | `{"id":1}` | 200 | `status` → `PendingApproval`(1) | P0 |
| IA-S-002 | 非草稿状态提交 | POST `.../Submit` | 状态为 `PendingApproval`(1) | 500 | 当前状态不允许提交 | P1 |

---

### 5.4 审核调整单 — `POST /api/services/app/InventoryAdjustment/Approve`

#### 正常请求

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| IA-A-001 | 审核通过 | POST `.../Approve` | `{"id":1,"isApproved":true}` | 200 | `status` → `Approved`(2) | P0 |
| IA-A-002 | 审核驳回 | POST `.../Approve` | `{"id":1,"isApproved":false,"rejectReason":"调整量有疑问"}` | 200 | `status` → `Rejected`(3)，可修改后重新提交 | P1 |

#### 业务异常

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| IA-A-003 | 非待审核状态审核 | POST `.../Approve` | 状态为 `Draft`(0) | 500 | 当前状态不允许审核 | P1 |
| IA-A-004 | 无权限审核 | POST `.../Approve` | 无 `Pages.InventoryAdjustment.Approve` | 403 | 权限不足 | P0 |

---

### 5.5 执行调整 — `POST /api/services/app/InventoryAdjustment/Execute`

#### 正常请求

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| IA-E-001 | 执行仅含盘亏项的调整单 | POST `.../Execute` | `{"id":1}` | 200 | `status`→`Executed`(4)，库存直接扣减，不创建预报单 | P0 |
| IA-E-002 | 执行仅含盘盈项的调整单 | POST `.../Execute` | `{"id":2}` | 200 | 按货主分组创建预报单，走全自动入库流程 | P0 |
| IA-E-003 | 执行混合差异调整单（盘盈+盘亏） | POST `.../Execute` | `{"id":3}` | 200 | 盘亏扣减 + 盘盈创建预报单，同时处理 | P0 |
| IA-E-004 | 盘盈多货主分组验证 | POST `.../Execute` | 盘盈项分属3个货主 | 200 | 生成3张预报单，每张按货主分组 | P1 |
| IA-E-005 | 盘盈无货主处理 | POST `.../Execute` | 盘盈项 `OwnerUserId=null` | 200 | 创建无主预报单（`UserId=空`） | P1 |

#### 业务异常

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| IA-E-006 | 非已审核状态执行 | POST `.../Execute` | 状态为 `PendingApproval`(1) | 500 | 当前状态不允许执行 | P0 |
| IA-E-007 | 货主余额不足导致执行失败 | POST `.../Execute` | 货主余额不足以支付盘盈入库费 | 500 | 执行失败（事务回滚），无库存变更 | P0 |
| IA-E-008 | 无权限执行 | POST `.../Execute` | 无 `Pages.InventoryAdjustment.Execute` | 403 | 权限不足 | P0 |
| IA-E-009 | 已执行调整单重复执行 | POST `.../Execute` | 状态为 `Executed`(4) | 500 | 不允许重复执行 | P1 |

---

### 5.6 盘盈自动入库验证（执行调整后的数据校验）

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| IA-V-001 | 预报单字段正确性 | 查询预报单列表 | 按 `Source=StockTakingSurplus(810)` 筛选 | 200 | `ForecastCode` 以"PDPY"开头，`Status=Stored(500)`，`StoreStatus=AllStore`，`ExternalOrderCode=调整单号` | P0 |
| IA-V-002 | 三层库存记录完整性 | 查询库存 | 按预报单ID关联查询 | 200 | `ExportContainerStoreTotalInfo` + `ExportContainerStoreInfo` + `ExportContainerStoreDetailInfo` 三层记录均存在 | P1 |
| IA-V-003 | 上架单验证 | 查询上架单 | 按预报单关联查询 | 200 | `WarehouseOnShelfBillInfo` 存在，上架库位=盘点原库位 | P1 |
| IA-V-004 | 费用收退验证 | 查询费用记录 | 按预报单关联 | 200 | 一笔扣费（payKind=0）+ 一笔等额退费（payKind=1） | P1 |
| IA-V-005 | 预报单商品明细映射 | 查询预报单商品 | 按预报单ID | 200 | `SKU`/`GoodsName`/`DeclaredStock`/`ActualStock`/`BatchNo`/`BarCode` 正确映射 | P1 |

---

### 5.7 取消调整单 — `POST /api/services/app/InventoryAdjustment/Cancel`

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| IA-C-001 | 取消草稿调整单 | POST `.../Cancel` | `{"id":1}` | 200 | `status` → `Cancelled`(5) | P1 |
| IA-C-002 | 取消待审核调整单 | POST `.../Cancel` | `{"id":2}` | 200 | `status` → `Cancelled`(5) | P1 |
| IA-C-003 | 取消已执行调整单 | POST `.../Cancel` | 状态为 `Executed`(4) | 500 | 已执行不允许取消 | P1 |

---

### 5.8 ERP同步验证

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| IA-ERP-001 | SyncToErp=true时执行调整 | POST `.../Execute` | 配置 `SyncToErp=true` | 200 | 差异数据推送至ERP | P2 |
| IA-ERP-002 | SyncToErp=false时执行调整 | POST `.../Execute` | 配置 `SyncToErp=false` | 200 | 不推送ERP | P2 |

---

## 6. 盘点设置（StockTakingSettings）

### 6.1 获取设置 — `GET /api/services/app/StockTakingSettings/Get`

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SS-G-001 | 获取当前设置 | GET `.../Get` | 无参数 | 200 | 返回全部配置项及当前值（复盘阈值、终盘阈值、超时配置、自动锁定、自动快照等） | P0 |
| SS-G-002 | 无权限查看设置 | GET `.../Get` | 无 `Pages.StockTakingSettings` 权限 | 403 | 权限不足 | P1 |

---

### 6.2 更新设置 — `POST /api/services/app/StockTakingSettings/Update`

#### 正常请求

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SS-U-001 | 修改复盘触发配置 | POST `.../Update` | `{"recountEnabled":true,"recountDiffQuantityThreshold":10,"recountDiffRateThreshold":10.0,"recountThresholdLogic":"AND","recountAssignRule":"Original"}` | 200 | 配置更新成功 | P1 |
| SS-U-002 | 修改终盘触发配置 | POST `.../Update` | `{"finalCountEnabled":true,"finalDiffQuantityThreshold":5,"finalDiffRateThreshold":5.0,"finalThresholdLogic":"OR"}` | 200 | 配置更新成功 | P1 |
| SS-U-003 | 修改超时配置 | POST `.../Update` | `{"claimTimeoutMinutes":30,"timeoutAction":"ReopenClaim"}` | 200 | 配置更新成功 | P2 |
| SS-U-004 | 开启自动锁定库存 | POST `.../Update` | `{"autoLockInventory":true}` | 200 | 后续生成任务自动冻结 | P1 |
| SS-U-005 | 关闭自动创建快照 | POST `.../Update` | `{"autoCreateSnapshot":false}` | 200 | 后续生成任务不创建快照，盘点明细仍正常创建 | P1 |
| SS-U-006 | 设置默认拆分规则 | POST `.../Update` | `{"defaultSplitDimension":"ByArea"}` | 200 | 后续默认按库区拆分 | P2 |

#### 参数边界

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SS-U-007 | 差异数量阈值=0 | POST `.../Update` | `recountDiffQuantityThreshold=0` | 200/400 | 阈值0表示任何差异都触发复盘，或参数校验失败 | P2 |
| SS-U-008 | 差异率阈值=100% | POST `.../Update` | `recountDiffRateThreshold=100.0` | 200 | 几乎不触发复盘 | P2 |
| SS-U-009 | 超时分钟数=0 | POST `.../Update` | `claimTimeoutMinutes=0` | 200/400 | 0表示不超时，或参数校验失败 | P2 |

#### 非法参数

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SS-U-010 | 差异数量阈值为负数 | POST `.../Update` | `recountDiffQuantityThreshold=-1` | 400/500 | 阈值不能为负数 | P1 |
| SS-U-011 | 差异率阈值为负数 | POST `.../Update` | `recountDiffRateThreshold=-5.0` | 400/500 | 阈值不能为负数 | P1 |
| SS-U-012 | 差异率阈值超过100 | POST `.../Update` | `recountDiffRateThreshold=150.0` | 400/500 | 阈值不能超过100% | P2 |
| SS-U-013 | 超时分钟数为负数 | POST `.../Update` | `claimTimeoutMinutes=-10` | 400/500 | 不能为负数 | P2 |
| SS-U-014 | 阈值条件逻辑传非法枚举 | POST `.../Update` | `recountThresholdLogic="XOR"` | 400 | 参数校验失败 | P2 |

#### 业务异常

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SS-U-015 | 无编辑权限修改设置 | POST `.../Update` | 无 `Pages.StockTakingSettings.Edit` 权限 | 403 | 权限不足 | P1 |

---

## 7. 盘点计费（StockTakingBilling）

### 7.1 预估费用 — `POST /api/services/app/StockTakingPlan/EstimateFee`

#### 正常请求

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SB-EF-001 | 预估收费盘点单费用 | POST `.../EstimateFee` | `{"id":1}` （`IsChargeable=true`，已配置报价方案） | 200 | 返回 `estimatedFee > 0`，`billingStatus` → `Quoted` | P1 |

#### 业务异常

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SB-EF-002 | 非收费盘点单预估 | POST `.../EstimateFee` | `IsChargeable=false` 的盘点单 | 500 | 非收费盘点单不支持预估 | P1 |
| SB-EF-003 | 无报价方案 | POST `.../EstimateFee` | 未配置仓库报价方案 | 500 | 缺少报价配置 | P1 |
| SB-EF-004 | 无权限预估 | POST `.../EstimateFee` | 无 `Pages.StockTakingPlan.Billing` 权限 | 403 | 权限不足 | P1 |

---

### 7.2 获取计费详情 — `GET /api/services/app/StockTakingPlan/GetBillingDetail`

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SB-BD-001 | 获取计费详情 | GET `.../GetBillingDetail` | `?id=1` | 200 | 返回 `estimatedFee`、`actualFee`、`billingStatus`、`billingTime` 等字段 | P1 |
| SB-BD-002 | 非收费盘点单获取详情 | GET `.../GetBillingDetail` | `IsChargeable=false` | 200 | `billingStatus=None`，费用相关字段为空/0 | P2 |

---

### 7.3 确认扣款 — `POST /api/services/app/StockTakingPlan/ConfirmBilling`

#### 正常请求

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SB-CB-001 | 确认扣款 | POST `.../ConfirmBilling` | `{"id":1,"remark":"费用确认"}` | 200 | `billingStatus` → `Paid`，生成 `ExportFeeDetailInfo` + 交易记录 | P1 |

#### 业务异常

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SB-CB-002 | 非Calculated状态确认扣款 | POST `.../ConfirmBilling` | `billingStatus=Quoted` | 500 | 费用未计算完成，不允许确认 | P1 |
| SB-CB-003 | 已扣款重复确认 | POST `.../ConfirmBilling` | `billingStatus=Paid` | 500 | 不允许重复扣款 | P1 |
| SB-CB-004 | 无确认权限 | POST `.../ConfirmBilling` | 无 `Pages.StockTakingPlan.BillingConfirm` 权限 | 403 | 权限不足 | P1 |
| SB-CB-005 | 货主余额不足 | POST `.../ConfirmBilling` | 货主余额不足 | 500 | 扣款失败，`billingStatus` → `Failed` 或保持 `Calculated` | P1 |

---

### 7.4 重新计算费用 — `POST /api/services/app/StockTakingPlan/RecalculateFee`

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SB-RF-001 | 重新计算费用 | POST `.../RecalculateFee` | `{"id":1}` | 200 | `actualFee` 更新 | P2 |
| SB-RF-002 | 已扣款后重新计算 | POST `.../RecalculateFee` | `billingStatus=Paid` | 500 | 已扣款不允许重算 | P2 |

---

### 7.5 计费方式验证

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SB-M-001 | 按SKU品种数计费 | 盘点完成后自动 | `StandardMode=SKUCategoryCost` | — | `actualFee = SKU品种数 × 单价` | P2 |
| SB-M-002 | 按库位数计费 | 盘点完成后自动 | `StandardMode=WarehousePositionCost` | — | `actualFee = 库位数 × 单价` | P2 |
| SB-M-003 | 按件数计费 | 盘点完成后自动 | `StandardMode=PieceCost` | — | `actualFee = 实盘总件数 × 单价` | P2 |
| SB-M-004 | 固定费用 | 盘点完成后自动 | `StandardMode=NumberOfTimesCost` | — | `actualFee = 固定金额` | P2 |

---

## 8. 库存冻结（InventoryFreeze）

### 8.1 查看冻结记录 — `GET /api/services/app/InventoryFreeze/GetAll`

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| IF-L-001 | 查看冻结记录列表 | GET `.../GetAll` | `?skipCount=0&maxResultCount=10` | 200 | 包含冻结/解冻时间、操作人、原因、当前状态 | P1 |

---

### 8.2 手动冻结 — `POST /api/services/app/InventoryFreeze/Freeze`

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| IF-F-001 | 手动冻结指定库存 | POST `.../Freeze` | `{"warehousePositionInfoId":1,"sku":"SKU001","reason":"盘点需要"}` | 200 | 冻结成功，`FreezeStatus=Frozen` | P2 |
| IF-F-002 | 无权限冻结 | POST `.../Freeze` | 无 `Pages.InventoryFreeze.Freeze` 权限 | 403 | 权限不足 | P2 |
| IF-F-003 | 冻结已冻结的库存 | POST `.../Freeze` | 库存已处于冻结状态 | 500 | 库存已冻结 | P2 |

---

### 8.3 手动解冻 — `POST /api/services/app/InventoryFreeze/Unfreeze`

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| IF-UF-001 | 手动解冻 | POST `.../Unfreeze` | `{"freezeId":1}` | 200 | 解冻成功，`FreezeStatus=Thawed` | P2 |
| IF-UF-002 | 无权限解冻 | POST `.../Unfreeze` | 无 `Pages.InventoryFreeze.Unfreeze` 权限 | 403 | 权限不足 | P2 |
| IF-UF-003 | 解冻未冻结的记录 | POST `.../Unfreeze` | 已是解冻状态 | 500 | 库存未冻结 | P2 |

---

### 8.4 自动冻结/解冻场景验证

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| IF-A-001 | 生成任务时自动冻结（IsFreezeInventory=true） | POST `.../GenerateTasks` | 盘点单 `isFreezeInventory=true` | 200 | 冻结记录自动创建，`FreezeStatus=Frozen` | P0 |
| IF-A-002 | 冻结期间出库被拒 | 尝试出库/拣货 | 冻结范围内的SKU | 500 | 操作被系统拒绝 | P0 |
| IF-A-003 | 冻结期间入库被拒 | 尝试入库 | 冻结范围内的SKU | 500 | 操作被系统拒绝，无期间流水产生 | P0 |
| IF-A-004 | 盘点完成自动解冻 | 盘点全流程完成 | 所有差异处理完毕 | — | 库存自动解冻，`FreezeStatus=Thawed` | P0 |
| IF-A-005 | 盘点作废自动解冻 | POST `.../Cancel` | 作废进行中的盘点单 | 200 | 库存自动解冻 | P1 |
| IF-A-006 | 不冻结时出入库正常+期间流水 | 正常出入库 | `IsFreezeInventory=false`，盘点进行中 | 200 | 出入库正常执行，期间流水B自动记录 | P1 |

---

## 9. 盘点记录查询（StockTakingRecord）

### 9.1 查询盘点记录列表 — `GET /api/services/app/StockTakingRecord/GetAll`

#### 正常请求

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SR-L-001 | 无条件查询记录列表 | GET `.../GetAll` | `?skipCount=0&maxResultCount=20` | 200 | 返回分页列表，每条含：盘点单号、任务编号、轮次、SKU、库位、A/B/C/D/E、差异类型、数据来源、盘点人、盘点时间 | P0 |
| SR-L-002 | 按盘点单号模糊搜索 | GET `.../GetAll` | `?planCode=ST2026` | 200 | 仅返回匹配的记录 | P1 |
| SR-L-003 | 按任务编号模糊搜索 | GET `.../GetAll` | `?taskCode=ST-T-001` | 200 | 仅返回匹配的记录 | P1 |
| SR-L-004 | 按SKU/产品名称模糊搜索 | GET `.../GetAll` | `?skuKeyword=SKU001` | 200 | 返回匹配记录 | P1 |
| SR-L-005 | 按库位编码精确筛选 | GET `.../GetAll` | `?positionCode=A-01-01` | 200 | 仅返回该库位的记录 | P1 |
| SR-L-006 | 按仓库精确筛选 | GET `.../GetAll` | `?warehouseInfoId=1` | 200 | 仅返回该仓库的记录 | P1 |
| SR-L-007 | 按盘点时间范围筛选 | GET `.../GetAll` | `?countTimeStart=2026-04-01&countTimeEnd=2026-04-30` | 200 | 返回该时间范围内的记录 | P1 |
| SR-L-008 | 仅显示差异项（OnlyDifference=true） | GET `.../GetAll` | `?onlyDifference=true` | 200 | 仅返回 `DifferenceQuantity ≠ 0` 的记录 | P0 |
| SR-L-009 | OnlyDifference=false | GET `.../GetAll` | `?onlyDifference=false` | 200 | 返回所有记录（含无差异） | P1 |

#### 参数边界

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SR-L-010 | 时间范围跨越多个盘点单 | GET `.../GetAll` | 大范围时间 | 200 | 正确返回跨盘点单的记录 | P2 |
| SR-L-011 | 多条件组合查询 | GET `.../GetAll` | `?planCode=ST2026&onlyDifference=true&warehouseInfoId=1` | 200 | 多条件交集结果 | P2 |

#### 非法参数

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SR-L-012 | countTimeStart 格式非法 | GET `.../GetAll` | `?countTimeStart=invalid` | 400 | 日期格式无效 | P2 |

---

### 9.2 导出盘点记录 — `GET /api/services/app/StockTakingRecord/Export`

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| SR-E-001 | 按当前条件导出Excel | GET `.../Export` | 与列表同筛选参数 | 200 | 返回Excel文件，包含所有列表字段 | P2 |

---

## 10. 权限控制（通用）

### 10.1 无权限访问接口验证

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| AUTH-001 | 无 `Pages.StockTakingPlan` 权限查看列表 | GET `.../StockTakingPlan/GetAll` | 无权限用户请求 | 403 | Forbidden | P0 |
| AUTH-002 | 无 `Pages.StockTakingPlan.Create` 权限创建 | POST `.../StockTakingPlan/Create` | 无权限用户请求 | 403 | Forbidden | P0 |
| AUTH-003 | 无 `Pages.StockTakingPlan.Edit` 权限编辑 | POST `.../StockTakingPlan/Update` | 无权限用户请求 | 403 | Forbidden | P1 |
| AUTH-004 | 无 `Pages.StockTakingPlan.Submit` 权限提交 | POST `.../StockTakingPlan/Submit` | 无权限用户请求 | 403 | Forbidden | P1 |
| AUTH-005 | 无 `Pages.StockTakingPlan.Approve` 权限审核 | POST `.../StockTakingPlan/Approve` | 无权限用户请求 | 403 | Forbidden | P0 |
| AUTH-006 | 无 `Pages.StockTakingPlan.Cancel` 权限作废 | POST `.../StockTakingPlan/Cancel` | 无权限用户请求 | 403 | Forbidden | P1 |
| AUTH-007 | 无 `Pages.StockTakingPlan.GenerateTasks` 权限生成任务 | POST `.../StockTakingPlan/GenerateTasks` | 无权限用户请求 | 403 | Forbidden | P0 |
| AUTH-008 | 无 `Pages.StockTakingTask` 权限查看任务 | GET `.../StockTakingTask/GetAll` | 无权限用户请求 | 403 | Forbidden | P0 |
| AUTH-009 | 无 `Pages.StockTakingTask.Assign` 权限分配 | POST `.../StockTakingTask/Assign` | 无权限用户请求 | 403 | Forbidden | P1 |
| AUTH-010 | 无 `Pages.StockTakingTask.Claim` 权限领取 | POST `.../StockTakingTask/Claim` | 无权限用户请求 | 403 | Forbidden | P1 |
| AUTH-011 | 无 `Pages.StockTakingTask.Execute` 权限录入 | POST `.../StockTakingExecution/Input` | 无权限用户请求 | 403 | Forbidden | P0 |
| AUTH-012 | 无 `Pages.StockTakingTask.Submit` 权限提交任务 | POST `.../StockTakingTask/Submit` | 无权限用户请求 | 403 | Forbidden | P1 |
| AUTH-013 | 无 `Pages.StockTakingDifference` 权限查看差异 | GET `.../StockTakingDifference/GetAll` | 无权限用户请求 | 403 | Forbidden | P0 |
| AUTH-014 | 无 `Pages.StockTakingDifference.Approve` 权限审核差异 | POST `.../StockTakingDifference/Approve` | 无权限用户请求 | 403 | Forbidden | P0 |
| AUTH-015 | 无 `Pages.InventoryAdjustment` 权限查看调整 | GET `.../InventoryAdjustment/GetAll` | 无权限用户请求 | 403 | Forbidden | P0 |
| AUTH-016 | 无 `Pages.InventoryAdjustment.Execute` 权限执行调整 | POST `.../InventoryAdjustment/Execute` | 无权限用户请求 | 403 | Forbidden | P0 |
| AUTH-017 | 无 `Pages.StockTakingSettings` 权限查看设置 | GET `.../StockTakingSettings/Get` | 无权限用户请求 | 403 | Forbidden | P1 |
| AUTH-018 | 无 `Pages.StockTakingSettings.Edit` 权限修改设置 | POST `.../StockTakingSettings/Update` | 有查看无编辑权限 | 403 | Forbidden | P1 |

---

### 10.2 多租户数据隔离

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| AUTH-019 | 租户A不可见租户B的盘点单 | GET `.../StockTakingPlan/GetAll` | 租户A登录 | 200 | 仅返回租户A的数据 | P0 |
| AUTH-020 | 租户A不可操作租户B的盘点单 | POST `.../StockTakingPlan/Approve` | 租户A操作租户B的盘点单ID | 500/403 | 拒绝操作 | P0 |
| AUTH-021 | 租户A不可见租户B的任务 | GET `.../StockTakingTask/GetAll` | 租户A登录 | 200 | 仅返回租户A的数据 | P0 |

---

### 10.3 盲盘接口安全

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| AUTH-022 | 盲盘模式任务详情不返回账面数量 | GET `.../StockTakingTask/Get` | 盲盘任务ID | 200 | 盘点明细中不返回 `systemQuantity(C)` 和 `snapshotQuantity(A)` 字段 | P0 |
| AUTH-023 | 盲盘模式录入响应不返回账面数量 | POST `.../StockTakingExecution/Input` | 盲盘任务录入 | 200 | 响应中不包含 `systemQuantity` | P1 |

---

### 10.4 子权限与父权限关系

| 用例ID | 场景描述 | 请求方法/URL | 请求参数（示例） | 预期状态码 | 预期响应体/字段 | 优先级 |
|--------|----------|-------------|-----------------|-----------|----------------|--------|
| AUTH-024 | 有Execute无Task查看权限 | POST `.../StockTakingExecution/Input` | 有执行权限但无查看权限 | 403 | 子权限不脱离父权限生效 | P1 |
| AUTH-025 | 有BillingConfirm无Billing查看权限 | POST `.../ConfirmBilling` | 有确认扣款权限但无计费查看权限 | 403 | 子权限不脱离父权限生效 | P2 |

---

## 附录A：已知缺陷关联用例

| Bug编号 | 描述 | 关联测试用例 | 预期行为 | 当前实际行为 |
|---------|------|-------------|----------|-------------|
| Bug#1 | ByOwner 拆分时 OwnerUserId=null 的库存静默丢失 | SP-G-017 | 无主库存纳入独立任务组 | 无主库存被静默忽略 |
| Bug#2 | BySKU 拆分时 SKU=null 的库存静默丢失 | SP-G-018 | 无SKU库存纳入独立任务组 | 无SKU库存被静默忽略 |
| Bug#3 | ByArea/ByOwner ScopeValue 必须是数字ID | SP-C-031, SP-C-032 | 传名称时应报错 | 静默解析失败，误报无库存 |
| Bug#4 | TotalPositionCount/TotalSKUCount 始终为 0 | SP-G-019 | 应从明细数据回填 | 始终为0 |

---

## 附录B：用例统计汇总

| 模块 | P0 | P1 | P2 | 合计 |
|------|----|----|-----|------|
| 盘点单管理（创建/编辑/提交/审核/生成/作废/列表/导入） | 26 | 38 | 22 | 86 |
| 盘点任务管理（分配/领取/提交/强制完成/取消/列表） | 7 | 16 | 9 | 32 |
| 盘点执行与扫描（录入/批量导入） | 5 | 10 | 5 | 20 |
| 差异单管理（列表/详情/审核/复盘/终盘/状态流转） | 9 | 10 | 4 | 23 |
| 库存调整单管理（列表/详情/提交/审核/执行/盘盈验证/取消） | 8 | 10 | 5 | 23 |
| 盘点设置 | 1 | 7 | 9 | 17 |
| 盘点计费 | 0 | 8 | 6 | 14 |
| 库存冻结 | 4 | 3 | 6 | 13 |
| 盘点记录查询 | 2 | 6 | 4 | 12 |
| 权限控制 | 12 | 9 | 2 | 23 |
| **合计** | **74** | **117** | **72** | **263** |
